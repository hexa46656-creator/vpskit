# VPSKit v1.0 Release Guide

1. Build the release archive with `scripts/release_packager.sh`.
2. Confirm the zip and checksum files are present.
3. Upload the archive to a GitHub release.
4. Publish the note from `RELEASE.md`.

The release process stays offline-safe and does not call external upload APIs.
